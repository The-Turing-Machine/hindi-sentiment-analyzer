{
  "name": "api",
  "version": "1.0.0",
  "description": "This is where all the node modules and JS code goes. This directory will run a server to make API calls.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hapi": "^13.0.0"
  }
}
